# ---- Frontend (Vite) lokale Entwicklung ----
# Basis-URL deines Spring-Boot-Backends
VITE_API_BASE=http://localhost:8080

# (Optional) Wenn du gegen Render testen willst, überschreibe oben:
# VITE_API_BASE=https://webtech-projekt-<dein-suffix>.onrender.com

# Nur Deko – kannst du im UI verwenden, z.B. document.title etc...
VITE_APP_TITLE=Movie / Series Tracker
